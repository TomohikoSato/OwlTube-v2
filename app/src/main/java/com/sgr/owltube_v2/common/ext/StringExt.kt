package com.sgr.owltube_v2.common.ext

/*
fun String.containsWithNormailze(target: String) {

    if (target.isKatakana(target)) {
        val hoge = convertToHiragana(target)
    }
}

fun convertToHiragana(str: String): String {
    val buf = StringBuffer()
    for (c: Char in str) {
        if (c.toInt() in 0x30a1..0x30f3) {
            buf.append((c.toInt() - 0x60).toChar())
        } else {
            buf.append(c)
        }
    }
}


fun String.isHiragana(target: String): Boolean {
    return (target.matches("^[\\u3040-\\u309F]+$".toRegex()))
}

fun String.isKatakana(target: String): Boolean {
    return (target.matches("^[\\u30A0-\\u30FF]+$".toRegex()))
}

*/
